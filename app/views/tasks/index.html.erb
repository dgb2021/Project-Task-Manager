<div class="container-sm">
  <div class="card">
    <div class="card-body">
      <h1>My tasks</h1>

      <% @tasks.each do |task| %>
        <ul class="list-group">
          <li class="list-group-item">
            <input class="form-check-input me-1" type="checkbox" value="" aria-label="...">
              <%= link_to task.name, task_path(task) %>
          </li>
        </ul>
      <% end %>
        <%= link_to "New", tasks_new_path, class: "btn btn-primary"  %>
    </div>
  </div>
</div>